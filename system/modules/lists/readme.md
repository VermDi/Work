Модуль для управления списками.

Изначально любой список создается в админ панели. Затем его можно использоваться с помощью класса:

    $lists = new mLists();
    $lists->getListsName('Каноническое имя');